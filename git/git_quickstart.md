```
# 创建git仓库
git init {repo_name}

# 添加文件或目录到仓库
git add {filename|dir}

# 提交到仓库
git commit -m '{message}'

# 查看当前版本状态
git status

# 查看提交日志
git log [--pretty=oneline]

# 查看所有操作
git reflog

# 查看将会被清理的文件，务必先使用-n查看会被清理的文件
git clean -n [-...]

```